# .env.test

# 64-character hex string for encryption (dummy value, safe for testing)
  ENCRYPTION_KEY=0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef
# JWT secret for signing test tokens (dummy value)
JWT_SECRET=testsecret

# Dummy OpenAI API key (not used in real API calls during tests)
OPENAI_API_KEY=test-openai-key

# Dummy Google OAuth credentials (not used in real API calls during tests)
GOOGLE_CLIENT_ID=test-google-client-id
GOOGLE_CLIENT_SECRET=test-google-client-secret
GOOGLE_REDIRECT_URI=http://localhost:3000/api/auth/callback